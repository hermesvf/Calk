<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity"
    >

    <item android:id="@+id/login" android:title="Notificaciones" android:orderInCategory="150"
        app:showAsAction="never" >
        <menu>
            <item android:id="@+id/toast" android:title="Toast" android:orderInCategory="150"
                app:showAsAction="never" />
            <item android:id="@+id/state" android:title="Estado" android:orderInCategory="50"
                app:showAsAction="never" />
        </menu>

    </item>
    <item android:id="@+id/logout" android:title="Logout" android:orderInCategory="250"
        app:showAsAction="never" />
</menu>

